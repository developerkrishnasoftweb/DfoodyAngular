export interface userLoginData {
    userName?: string,
    displayName?: string,
    country: string,
    jwtToken?: string
    error?: null,
    firstName?: string,
    roles?: [ ],
    surname?: string
}
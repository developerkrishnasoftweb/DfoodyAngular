export interface ICurrentUser {
    userId?: string;
    branchType?: string;
    branchCode?: string;
    branchName?: string;
}

export interface ICurrency {
    countryId: string;
    currencyCode: string;
    symbol: string;
}